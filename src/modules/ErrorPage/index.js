import React from 'react';

const ErrorPage = () => {
    return (
        <main className="main-container">
            <div>Oops, cannot fetch data at the moment!</div>
        </main>
    );
};

export default ErrorPage;
